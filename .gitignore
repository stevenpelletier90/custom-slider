node_modules/

# Claude Code — shared config is tracked, personal config is local-only.
# Shared & tracked: settings.json (wires the PostToolUse format hook)
.claude/settings.local.json
.claude/todos/
